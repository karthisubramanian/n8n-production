# Environment variables for n8n production deployment
# Copy this file to .env and update the values

# Database Configuration
DB_DATABASE=n8n_production
DB_USERNAME=n8n_user
DB_PASSWORD=your_secure_db_password_here

# n8n Configuration
N8N_HOST=n8n.gdart.in
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=your_secure_admin_password_here
N8N_ENCRYPTION_KEY=your_32_character_encryption_key_here

# Optional Configuration
N8N_LOG_LEVEL=info
TIMEZONE=UTC

# Security Notes:
# 1. Generate a strong encryption key: openssl rand -base64 32
# 2. Use strong passwords for database and basic auth
# 3. Keep this file secure and never commit it to version control
# 4. Consider using a password manager to generate secure passwords